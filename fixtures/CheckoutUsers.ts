interface CheckoutUsers {
    firstName: string,
    lastName: string,
    postalCode: string
};

export const checkoutUsers : CheckoutUsers = {
    firstName: "John",
    lastName: "Doe",
    postalCode: "12345"

};