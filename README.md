# sdet-portfolio

## Project Structure

As the project is quite small, interface and test data are kept within one file (fixtures > Users.ts)
On bigger projects, those would be separated into dedicated "types" (interface) and "helpers" (UserData) folders.